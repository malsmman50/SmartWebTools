# SmartCalcTools - Project Handover & Context Document

هذا الملف عبارة عن "دليل سياق شامل" (Context Document) مصمم ليتم تقديمه لأي ذكاء اصطناعي أو مطور يبدأ العمل على المشروع في محادثة جديدة. يحتوي على جميع التفاصيل المعمارية، قواعد البرمجة، والقرارات الفنية التي اتخذناها لضمان استمرارية العمل بنفس الكفاءة.

---

## 1. هوية المشروع (Project Identity)
- **الاسم:** SmartCalcTools (أدوات الحساب الذكية).
- **الهدف:** منصة تقدم أدوات مالية إسلامية، أدوات للمطورين، وحاسبات يومية وصحية، مع التركيز الشديد على السرعة، الخصوصية، وتجربة المستخدم (Premium UI/UX).
- **التقنيات المستخدمة:** 
  - Next.js 14 (App Router).
  - React (Client-Side primarily).
  - Vanilla CSS (`app/globals.css`) مع تصميم زجاجي (Glassmorphism) وألوان حيوية داكنة وفاتحة (Light/Dark Mode). *نحن لا نستخدم TailwindCSS*.

## 2. المبادئ الهندسية الأساسية (Core Engineering Principles)
1. **الخصوصية المعدومة (Zero-Trust & Privacy-First):** 
   - جميع الأدوات (بما فيها ضغط الصور، ومفكك JWT، ومحرك البحث Semantic ChatPDF، وحاسبات التمويل) تعمل 100% داخل متصفح المستخدم (Client-Side). لا يتم إرسال أي بيانات حساسة إلى أي سيرفر.
2. **العمل بدون إنترنت (PWA - Offline Ready):** 
   - الموقع مبرمج كتطبيق ويب تقدمي (Progressive Web App). يحتوي على `sw.js` (Service Worker) يقوم بتخزين ملفات الموقع في ذاكرة الهاتف (Cache). 
   - *ملاحظة مهمة:* بسبب الـ PWA، فإن أي تحديثات جديدة يتم رفعها قد تتطلب من المستخدم عمل "Hard Refresh" أو إغلاق التبويب وفتحه لكي يسحب التحديث الجديد.
3. **التصميم الجمالي (Rich Aesthetics):** 
   - واجهة المستخدم يجب أن تكون "مبهرة" (Wow-factor). يتم استخدام حركات دقيقة (Micro-animations)، تدرجات لونية هادئة، وبطاقات ذات حواف ناعمة. لا يُقبل التصميم البدائي.
4. **الأمان العالي (Strict CSP):** 
   - ملف `next.config.mjs` يحتوي على Content Security Policy (CSP) صارم جداً لحماية الموقع من ثغرات XSS. 
   - *تم تحديثه مؤخراً* للسماح بنطاقات `Google Analytics` (`googletagmanager.com` و `google-analytics.com`) لتعمل بدون أن يتم حظرها.

## 3. الهيكلة المعمارية للأدوات (Routing & Architecture)
اعتمدنا هيكلة متوافقة جداً مع الـ SEO (تحسين محركات البحث):
- `/calculators/islamic-finance`: أدوات التمويل الإسلامي (الزكاة، المرابحة، المضاربة، الصكوك، التقاعد الإسلامي FIRE).
- `/calculators/shopping`: أدوات التسوق (حاسبة الخصم والضريبة، محول مقاسات الأحذية العالمي).
- `/calculators/lifestyle`: أدوات الحياة اليومية (حاسبة تقسيم الفاتورة وتقشيش المطاعم).
- `/calculators/health`: أدوات الصحة (حاسبة الجسم الشاملة BMI/BMR/TDEE، حاسبة وتتبع الحمل مع تحذيرات YMYL الطبية).
- `/tools`: أدوات المطورين والإنتاجية (مفكك JWT، منسق JSON، ضاغط الصور، بوصلة القبلة، ومولد كرون).
- `/compare`: صفحات المقارنات المالية (مثل المرابحة ضد القرض التقليدي).

## 4. تقنيات الـ SEO المتقدمة المستخدمة
1. **Programmatic SEO (PSEO):**
   - قمنا ببناء نظام توليد صفحات ديناميكية (Dynamic Routes) لآلاف الكلمات المفتاحية (Long-tail Keywords).
   - مثال: مجلد `app/[lang]/calculators/shopping/shoe-size/[slug]` يقوم بتوليد مئات الصفحات أوتوماتيكياً (مثل `eu-40-to-us-men` وغيرها) بناءً على ملفات JSON مخزنة في `app/data/`.
2. **YMYL (Your Money or Your Life):**
   - في الأدوات الطبية (مثل حاسبة الحمل) والأدوات المالية (مثل الزكاة)، نستخدم تحذيرات صارمة (Medical/Financial Disclaimers) ونقدم بيانات مفصلة (مثل حجم الجنين الأسبوعي) لتجنب تقييم "المحتوى الضعيف" (Thin Content) من جوجل.
3. **الدعم ثنائي اللغة (Bilingual - ar/en):**
   - الموقع يدعم العربية والإنجليزية عبر مسار `/[lang]/`.
   - الاعتماد الكامل على قواميس محلية `app/dictionaries/ar.json` و `en.json`.
4. **ربط الإحصائيات (GA4):**
   - تم استخدام أحدث مكتبة `@next/third-parties/google` لدمج (Google Analytics) في `layout.js` لمراقبة سلوك الزوار والـ Retention دون التسبب ببطء التحميل.

## 5. خطة العمل المستقبلية (Roadmap للمحادثة القادمة)
عند بدء محادثة جديدة، يجب مراجعة هذا الملف ثم يمكن الانطلاق في أحد المسارات التالية:
1. **توسيع الـ PSEO:** إضافة حاسبات جديدة قابلة للتوليد الديناميكي (مثل تحويل العملات مع PSEO لكل أزواج العملات).
2. **تحليل البيانات:** استخدام بيانات GA4 لمعرفة أي الأدوات تستقبل أكبر عدد من الزوار وتطوير ميزات إضافية لها (Upscaling).
3. **تحقيق الدخل (Monetization):** الاستعداد لدمج إعلانات (Google AdSense) ضمن الـ CSP، أو تطوير أدوات مدفوعة (Freemium) ضمن قسم المطورين.
4. **تطوير المحتوى:** إضافة مقالات مالية وصحية متخصصة في مسار `/blog` لدعم الأدوات من خلال الروابط الداخلية (Internal Linking).

---
*نهاية الدليل. يمكنك الآن استخدام هذا الملف كسياق لأي وكيل ذكاء اصطناعي (AI Agent) جديد للبدء فوراً من حيث انتهينا.*
